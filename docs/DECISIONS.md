# Decision log - altaryslabs.com

Every structural decision, editorial call, design choice or scope decision is recorded here as a D-row before the session that made it closes, citing the work item it came from.

**Before assigning a new D-number**, grep for the highest one across **every open branch**, not only the current one. D-numbers collide the same way work-item IDs do:

```
grep -rhoE "^\| D[0-9]{3}" docs/DECISIONS.md | sort -u | tail -1
git branch -a --format='%(refname:short)' | while read b; do git show "$b:docs/DECISIONS.md" 2>/dev/null; done | grep -oE "^\| D[0-9]{3}" | sort -u | tail -1
```

**A D-row is a dated journal entry.** It is annotated when it is superseded, never rewritten. A clause made obsolete by a later decision is not a defect; it is the record doing its job. Add a new row that supersedes it and say so in the Rationale column.

Nomenclature, work item types and prefixes: see `docs/AI_Development_Workflow.md`.

---

| D | Date | Item | Decision | Rationale | Rejected alternatives |
|---|---|---|---|---|---|
| D001 | 2026-07-30 | (pre-dates the nomenclature) | Adopt an explicit AI-assisted development workflow, documented in `docs/AI_Development_Workflow.md` | Work was being done without a written contract on gates, branches or review. The failure modes on a marketing site are silent credibility defects, which need a checklist to be caught at all. | Keep working ad hoc; adopt the full seven-personality pipeline from renew-insurance |
| D002 | 2026-07-30 | (pre-dates the nomenclature) | Two personalities only: TECH_LEAD and REVIEWER | No legal exposure in a marketing page, no data model, no cross-module architecture. Seven personalities would be ceremony without return. Revisit if the site grows a real application surface. | The seven-personality pipeline; a single all-in-one personality with no independent review |
| D003 | 2026-07-30 | (pre-dates the nomenclature) | Writer / Reviewer separation is non-negotiable: the REVIEWER is always a separate, fresh session | An author reviewing their own work is biased toward the choices they just made. This is the single rule that buys the most defect detection. | Self-review with a checklist; review by the same session after a context reset |
| D004 | 2026-07-30 | (pre-dates the nomenclature) | Work item nomenclature STORY / FIX / REF / CHR, with the site-specific prefixes SITE, PAGE, UI, I18N, SEO, FORM, OPS | Defects, internal-quality changes and repo hygiene are recurring work; without their own IDs they end up mislabelled as stories or untracked entirely. | Story-only tracking; free-form branch slugs with no IDs |
| D005 | 2026-07-30 | (pre-dates the nomenclature) | Maximal bar on latent defects: the REVIEWER blocks on any verified defect regardless of whether the PR introduced it | Deferring a real defect silently is how it gets forgotten. Only the founder may consciously accept one as tracked debt. | Bounded bar, where only defects introduced by the PR block |
| D006 | 2026-07-31 | `SITE-CHR-001` | Adopt CLI launchers `bin/tech-lead`, `bin/reviewer`, `bin/autonomous_reviewer` plus the `/tech-lead` and `/review` slash commands | A personality that has to be pasted by hand drifts between sessions. A launcher makes the seeding reproducible. | Pasting the personality per session; a single generic launcher taking a role argument |
| D007 | 2026-07-31 | `SITE-CHR-001` | The stable review procedure lives in `.claude/reviewer-append.txt`, shared by both reviewer launchers and injected with `--append-system-prompt` | Keeps the invocation down to `<ID> (PR #<num>)`. The procedure cannot silently differ between an attended and an unattended round, which is exactly where drift would hide. | Restating the procedure in each prompt; duplicating it in both scripts |
| D008 | 2026-07-31 | `SITE-CHR-001` | The autonomous reviewer is authorized by a targeted allowlist in `.claude/autonomous-reviewer-settings.json`, never by `--dangerously-skip-permissions` | The allowlist grants writes to `docs/reviews/**` and nothing else. "The reviewer is read-only on source" becomes a structural guarantee rather than an instruction it could talk itself out of. | `--dangerously-skip-permissions`; attended review only |
| D009 | 2026-07-31 | `SITE-CHR-001` | Documentation writes are delegated to the `file-writer` Haiku subagent once the content is final; source files are written directly | Persisting a long document does not need Opus. Drafting does. The split keeps reasoning capacity where it matters. | Writing everything with Opus; delegating drafting as well as persistence |
| D010 | 2026-07-31 | `SITE-CHR-001` | The decision log lives at `docs/DECISIONS.md` in a single flat table | The project is small enough that one table stays readable. A per-module ledger would be premature structure. | One log per area; no log at all, relying on commit messages |
| D011 | 2026-07-30 | PR #2 | Astro 7 in static output on Cloudflare Pages, no adapter, no UI framework | Same pattern as the rest of the ALTARYS ecosystem (papillon-collection.com, monassurance.africa, mon-echeance.com); a marketing site needs no server runtime | Staying on a hand-written one-page site served by GitHub Pages; a JS framework such as Next.js |
| D012 | 2026-07-30 | PR #2 | French sits at the root, English under `/en/` with translated slugs; `src/i18n/routes.ts` is the single source of truth for routing | Slugs are translated, so no URL can be derived from the other language: hreflang, the language switcher and the footer must all read the same table | `/fr/` prefix on both languages; deriving the English URL from the French one by string manipulation |
| D013 | 2026-07-30 | PR #6 | The `Dictionary` type is derived from `fr.ts`, so a missing English key fails the build | Bilingual parity is the credibility risk on this site; a type error at build time is the only check that never forgets | Runtime fallback to French; a manual parity checklist |
| D014 | 2026-07-30 | PR #6 | `en.ts` is a re-adaptation, not a translation, addressing anglophone Africa, international investors, and donors and NGOs | OHADA and CIMA carry no meaning in Accra or Nairobi, and CNPS or ITS even less; the English page must argue offline-first and multi-country coverage instead | A literal translation of the French copy |
| D015 | 2026-07-30 | PR #4, PR #5 | Palette restricted to navy and gold; amber and teal removed from the tokens, violet forbidden | Amber and teal belonged to a product split that no longer exists; violet is reserved for altarys.ai, which is not launched | Keeping the amber and teal accents of the legacy one-page site |
| D016 | 2026-07-30 | PR #4 | Products before Services in the navigation | Deliberate positioning: the visitor is an executive evaluating a supplier, and PCS is the strongest current commercial proof | Services first, which the consulting revenue mix would have suggested |
| D017 | 2026-07-30 | PR #6 | Three products only; ALTARYS ENTERPRISE is dropped, and the name "Papillon Corporate Finance Suite" is kept | ALTARYS ENTERPRISE was replaced by Papillon Corporate Finance; the design handoff suggested renaming the suite, which the founder refused | The shorter "Papillon Corporate Finance" proposed by the mockups |
| D018 | 2026-07-30 | PR #5, PR #6 | PCS availability is written "Disponible T3 2026" and lives in a single dictionary constant, `availability.pcs` | No exact date on a public page; one line to change on launch day, and no risk of two pages disagreeing | The "mi-aout 2026" wording of the redesign spec, an exact date |
| D019 | 2026-07-30 | PR #6 | One content component per page, shared by both languages, on the `HomeContent` model | The structure of a page cannot be allowed to diverge between French and English; only the dictionary changes | Duplicating the markup in each language page |
| D020 | 2026-07-30 | PR #11 | The RCCM number is CI-ABJ-03-2026-B17-00070 | `CI-ABI` was a typo carried by several supports, corrected across all 26 pages | Keeping the `CI-ABI` form used by the design handoff |
| D021 | 2026-07-30 | PR #1 | The D1 binding stays commented out in `wrangler.jsonc` until the contact form ships, and D1 storage is treated as provisional | A block with an unresolved `database_id` fails the Cloudflare build; relocating storage to Africa is targeted for late 2026, so the storage layer must be isolated in a single module | Creating the database immediately; committing a placeholder identifier |
| D022 | 2026-07-30 | PR #10 | Legal notice and privacy policy are adapted from papillon-collection.com and published pending review by a lawyer | The site needs both pages to be credible; the texts are structurally sound and the risk of publishing them provisionally is low | Publishing no legal page until a lawyer has reviewed the copy |
| D023 | 2026-07-31 | PAGE-001 | The About page follows the design handoff layout (lede, leadership card, legal line, call to action) plus a three-principle "our approach" block inserted between the lede and the leadership card | The prototype alone made About the thinnest page of the site, on precisely the page an executive reads when evaluating a supplier; the three principles are sourced from `platform-saas-blueprint.md` and claim nothing new | Strict fidelity to the prototype; the fuller seven-block structure of the approved plan, which duplicated the Products and Services hubs |
| D024 | 2026-07-31 | PAGE-001 | The first principle is titled "Conformite OHADA et CIMA" in French and "Regional compliance" in English | Founder call: the French page names the two zones explicitly, while the English readaptation keeps the acronyms out of a card title, per D014 | "OHADA natif", proposed at the plan gate |
| D025 | 2026-07-31 | PAGE-001 | The About page names one person, Emmanuel Blonvia, Fondateur et President, and departs from the redesign spec which also listed a Directeur Projets et Transformation | Locked founder decision; no org chart, no headcount, no bios | Naming the second role, as `PROMPT_altaryslabs-com-refonte.md` section 4.10 still does |
| D026 | 2026-07-31 | PAGE-001 | The claim that the team combines banking, insurance and complex secure business systems experience is published as written in the design handoff | Founder confirmed the claim is accurate; it names nobody, quotes no figure and matches the sentence already live on the home page | Dropping the sentence; keeping it without naming the sectors |
| D027 | 2026-07-31 | PAGE-001 | The legal line on the About page is composed from `footer.legalEntity` and `footer.rccm` rather than from its own dictionary key, and rendered in `--muted-soft` instead of the footer's `--ink-25` | A registration number duplicated in two keys will eventually disagree; and at 10.5px, `--ink-25` falls below the WCAG AA contrast threshold for body content | A dedicated `about.legalLine` key; reusing the footer's exact styling |
| D028 | 2026-07-31 | UI-001 | The palette gains a second surface family: cream `#F6F2E9` pages with white cards, alternating with the existing navy sections. The foundation ships first, dormant; the default background flips in UI-002 | The July 2026 handoff turns the site light. Flipping the default and converting the pages cannot be gradual: a light header over pages still dark is not a shippable intermediate state, even on a preview URL. Splitting foundation from switch keeps every merge coherent | A single big-bang restyle in one pull request; a page-by-page conversion leaving the preview visibly mixed for several merges |
| D029 | 2026-07-31 | UI-001 | `--gold-65` is retired as a text colour and kept for borders and decoration. Eyebrows become `--gold-light` on dark and `--gold-deeper` on light. Small muted text becomes `--ink-60` on dark and `--slate-body` on light | Eleven occurrences measured below the WCAG AA threshold of 4.5:1, from 2.09:1 to 3.61:1, of which nine actually render and two sit in a `LegalPage` rule that a specificity collision already neutralises. They are one defect, not eleven: the same "small text in translucent gold or ivory" pattern. One rule fixes the family and stops it recurring | Fixing only the three occurrences the R1 review found; accepting them as tracked debt and deferring to a separate UI-FIX item |
| D030 | 2026-07-31 | UI-001 | The repository deliberately diverges from the handoff colour values wherever they fail WCAG AA: `#B8791E` small text becomes `#8B5E1B`, `#8A93A6` muted text becomes `#5B6472`, `#9AA3B4` placeholders become `#5B6472` | The redesign does not fix the accessibility defect, it reproduces it on a light background: 3.25:1 for eyebrows on cream, 3.63:1 for card links on white, 2.76:1 for the legal line. Each divergence is commented with its measured ratio in `tokens.css` so no future session re-harmonises it on the mockup without knowing why | Taking the handoff values as-is; keeping the handoff values and adding a contrast exception note |
| D031 | 2026-07-31 | UI-001 | `CLAUDE.md` is amended: two surface families, the two accessibility rules, "Papillon Corporate Finance Suite" restored, and the visual reference repointed to the project ROOT rather than the frozen `design_handoff_altaryslabs_refonte/` folder | `CLAUDE.md` is the authority that wins over every other document, so a lie in it propagates. The handoff drops "Suite" in five files and keeps it in `contact.dc.html`, contradicting itself; the founder's refusal recorded in D017 is the tiebreaker. The frozen folder had already caused the About page to be built all-navy on the day the redesign landed | Correcting the product name in its own one-line FIX item, which would have put two branches in `CLAUDE.md` at once and reproduced the `DECISIONS.md` conflict |
| D032 | 2026-07-31 | UI-001 | The legal notice and privacy policy texts are LOCKED. The handoff changes them and those changes are not taken. Only the surface restyle applies, plus the move of their two links into the footer's new Legal column | Founder ruling. The handoff's legal copy is thinner than what was shipped in PR #10, and its privacy page is a placeholder saying the policy "is being finalised". Adopting it would be a content regression on pages that are already pending a lawyer's review under D022 | Adopting the handoff copy; merging the two versions |
| D033 | 2026-07-31 | SITE-FIX-001 | `REVIEWER.md` no longer delegates its correctness pass to a `code-review` skill. The sweep is spelled out inline: dead imports, unhandled rejections, CSS rules that never apply, hardcoded values where a token exists, stale strings left by copy-paste between languages | The skill is not installed in this repository and cannot be invoked from an unattended session, so the personality was mandating an impossible step. A reviewer that cannot perform a mandatory step either stops or quietly skips it; both are worse than an explicit checklist | Installing a `code-review` skill; dropping the correctness pass entirely |
| D034 | 2026-07-31 | SITE-FIX-001 | Review captures go through `bin/review_shots`, which drives ONE Chrome instance over the DevTools protocol for every page and width | Relaunching Chrome per capture was the dominant cost of a review round. It is also the only correct way to get a mobile viewport: `--window-size` on headless Chrome is floored at 500px, so a 360px capture silently renders at 500px and crops the right-hand side, which reads as a layout overflow that does not exist | One Chrome per capture, as before; a screenshot service; skipping the mobile widths |
| D035 | 2026-07-31 | SITE-FIX-001 | **Rejected**: reusing the author's `node_modules` in the review worktree instead of running `npm ci`. `npm ci` stays | Measured twice on the target machine: `npm ci` takes 3.7s, copying `node_modules` takes 6.3s. npm hardlinks from its 14 GB local cache while `cp` moves 228 MB of real bytes, so the "optimisation" is 1.7 times SLOWER. It would also have cost the only check that the lockfile is coherent and that the site builds from nothing, which is exactly what Cloudflare Pages does | Copying `node_modules`; hardlinking it, rejected as well since two builds would then share Vite's cache directory |
| D036 | 2026-07-31 | UI-001 | The light peritext is written `.section--light.section--light`, the class deliberately repeated to buy one level of specificity | Without it the peritext ties with component-scoped styles, both at (0,2,0), and the winner is decided by emission order. Astro emits some component styles before the global bundle and some after, and three components switch sides depending on the route: `ProductCard` before global on /produits and after on /, `CtaBanner` before on /a-propos and after on sixteen other pages. The peritext was winning by luck. UI-002 will extend the whole site on this foundation, so it must win by construction | Relying on emission order; `@layer`, rejected because unlayered styles beat layered ones, so putting global.css in a layer would make it lose to component styles rather than win |
| D037 | 2026-07-31 | UI-001 | The list of points where the prototypes are wrong gains a sixth entry: the English hero was left on the old positioning while the French one changed | `CLAUDE.md` announced six points and listed five. A future author rebuilding an English page from the prototype would have silently reverted the hero while believing they followed the contract | Lowering the announced count to five, which would have left the trap in place |
| D038 | 2026-07-31 | UI-FIX-001 | The `LegalPage` specificity collision gets its own tracked item rather than a fix inside UI-001 | Restoring `.legal-updated` and `.legal-prevails` changes what four public pages render, which needs its own visual verification. UI-001 scoped itself to changing nothing that renders. The R1 review accepted the split and required only that the follow-up item exist before UI-001 merges | Fixing it inside UI-001; leaving it as an undocumented comment, which is how it survived until now |
| D039 | 2026-07-31 | UI-001 | On light surfaces `.btn-primary` keeps the handoff's gold fill and gains a `1px solid var(--gold-deep)` border | The gold fill is 2.47:1 against cream, below the 3:1 that SC 1.4.11 requires for a component boundary and that this same PR applies to the secondary button and the focus ring. The label stays readable at 6.86:1; it is the button's edge against the page that disappears. Darkening the fill would also have met the threshold, at the cost of a visible departure from the handoff on the first commercial call to action of every page | Darkening the fill on light; recording the gap as accepted debt and leaving the button unreadable at its edge |
| D040 | 2026-07-31 | UI-001 | The light peritext covers component-owned classes too, not only global primitives, and any new colour-bearing component class must be added to it as it is written | Eight component classes measured between 1.02:1 and 2.91:1 once the foundation was woken up. A foundation covering only part of them would force UI-002 to rediscover the rest page by page, which is precisely what a foundation item exists to prevent | Declaring the eight classes out of scope and handing them to UI-002 |
| D041 | 2026-07-30 | PR #9 | The HR page keeps the softened OHADA coverage wording, without "MVP" jargon and without a firm commitment to 17 countries by 2028 | Back-filled: the decision was taken in the commit "Adoucir l'engagement de couverture OHADA" and existed nowhere else. `CLAUDE.md` cites it as one of the six points where the prototypes are wrong, so it needs a row like the other five | Leaving it in a commit message only, where the next author rebuilding the page from the prototype would not find it |
| D042 | 2026-07-31 | I18N-001 | The home hero adopts the handoff wording, "Votre partenaire technologique pour les entreprises Africaines.", but keeps "Zones" in the plural against the prototype's singular | Founder call on positioning: "partenaire" addresses an executive evaluating a supplier, where "solutions" addressed a software buyer. OHADA and CIMA are two distinct zones, so the prototype's singular is a slip, not an intention | Keeping "Des solutions technologiques pour les entreprises d'Afrique."; following the prototype's singular "Zone" |
| D043 | 2026-07-31 | I18N-001 | The English hero is rewritten to "Your technology partner for businesses across Africa." with "OHADA and CIMA regions", wording the handoff never produced | The handoff changed the French hero and left the English one on the old "Technology solutions" positioning. Shipping the French change alone would have left the two languages arguing different things on the same page | Leaving the English hero as the handoff did; translating the French line literally, which D014 forbids |
| D044 | 2026-07-31 | I18N-001 | The consulting service is renamed "Conseil IT, RH & Corporate Finance" and "IT, HR & Corporate Finance Consulting" | Founder call, against the recommendation to keep the existing name. The handoff uses three different spellings across its files, so one had to be made canonical. No URL changes: the slugs stay `/services/conseil` and `/en/services/consulting` | Keeping "Conseil IT, RH et Finance", already live on 26 pages; the header and footer variant "Conseil IT, RH & Finance" |
| D045 | 2026-07-31 | I18N-001 | The English leadership role becomes "Founder and Chairman"; the French stays "Fondateur et President" | Founder call, against the recommendation to keep "Founder and President". The asymmetry is legitimate under D014: `en.ts` is a readaptation, not a translation | "Founder and President" in both languages, aligned on D025 |
| D046 | 2026-07-31 | OPS-001 | Of the two Cloudflare Pages projects connected to this repository, `altaryslabs-com` is deleted and `altaryslabscom` is kept | `altaryslabs-com` builds `main`, which still carries the legacy one-page site and no Astro build, so it has posted a red check on every pull request since #12. A check that is always red teaches everyone to ignore checks. The survivor is the project that builds green and whose preview URL validated every page of the rebuild | Keeping the hyphenated name because it reads closer to the domain and repointing it at the rebuild branch, rejected because repairing a project that never worked is riskier than keeping one that does; keeping both and muting one check |
| D047 | 2026-07-31 | OPS-001 | The surviving project's production branch is switched to `main` **after** the rebuild is merged, never before | Repointing first would make the surviving project's production build fail immediately, since `main` still has no Astro build, and it is precisely the project the custom domains are about to be attached to. Merge, verify `main` builds green as a preview, then switch, then attach the domains, then remove the GitHub Pages DNS records | The founder's initial sequence, which switched the branch before the merge |
| D048 | 2026-07-31 | UI-001 | Light-surface coverage is verified by `bin/contrast_sweep`, which wakes the foundation on every page and measures every text element against its effective background, rather than by reading the components | D040 recorded a coverage the shipped CSS did not have, and three consecutive review rounds found classes that reading had missed: twelve on the third, including `:global(strong)` at 1.04:1 which made "ALTARYS LABS" invisible on cream. Enumerating by hand does not converge. The tool was validated against the pre-fix build, where it reproduced all twelve findings plus four the review had not listed, then returned zero over the 26 pages after the fix | Continuing to enumerate by reading; trusting the reviewer to find what the author missed, which is what had been happening |
| D049 | 2026-07-31 | (backlog) | The remaining work is decomposed into `UI-002`, `PAGE-002`, `FORM-001` and `OPS-002`, in that dependency order, indexed by `docs/BACKLOG.md` | The rebuild has more sessions ahead of it than any one conversation can carry, and a session that resumes cannot read the one that stopped. Ordering matters and is not obvious: the Contact page waits on the light switch because its prototype is a light page, and the form waits on the page. Writing the order down is what stops it being re-derived, wrongly, later | Keeping the remaining work in conversation and in the founder's head; one large item covering the whole restyle, which would have been unreviewable |
| D050 | 2026-08-02 | UI-FIX-002 | The three structural suggestions of the PAGE-001 R1 review become a FIX rather than being dropped: the orphan "Notre approche" eyebrow and the four grids that lose list semantics under `list-style: none` | They were reported non-blocking, which is correct for a sighted visitor and wrong for a screen-reader user, who is a real user. An intended behaviour exists, the structure announcing itself the way it reads, and it is wrong. The four grids are fixed in one pass because fixing one is worse than fixing none: it breaks the house convention without making the site accessible | Leaving them as suggestions, which is how they were already lost once; fixing only the About grid |
| D051 | 2026-08-02 | UI-REF-001 | `.card-text.is-last` is promoted to `global.css` next to `.card-text`, and every scoped copy is removed | The modifier is duplicated across components because Astro scoped styles cannot reach a sibling, but `.card-text` itself is a global primitive, so its modifier belongs beside it. Left alone, the third component that needs it will redeclare it and the copies will drift. Filed as a REF because a visitor sees exactly the same page | Leaving the duplication as an accepted cost of scoped styles; sweeping the whole styling architecture at the same time |
| D052 | 2026-08-02 | SITE-FIX-002 | The review worktree procedure is a FIX, not a hardening: `git worktree add <path> <branch>` silently detaches when the branch is checked out elsewhere, and the failure only surfaces eight steps later when the review push has no branch to update | Observed on the PAGE-001 R1 round, where the worktree sat at `HEAD (no branch)` and the reviewer had to improvise a `review-page-001` branch to save the review. The procedure did not save it, the reviewer did. A guard on `git symbolic-ref -q HEAD` is the part that matters: it turns a silent failure at the last step into a loud one at the first | Treating it as a reviewer skill issue; documenting the workaround without a guard |
| D053 | 2026-08-02 | SITE-CHR-002 | The stale PAGE-001 review is closed by running an independent round 2, never by editing round 1's boxes | All four findings are resolved on `d96b71f`, but the file still asserts CHANGES REQUESTED in the present tense, so the next reader either re-investigates four resolved findings or re-fixes correct code. Round 1 stays exactly as written: a merged record is annotated, never rewritten, and its unchecked boxes are what was true on 31 July | Ticking the boxes by hand, which would rewrite history and bypass the Writer / Reviewer separation; deleting the file |
| D054 | 2026-08-03 | SITE-FIX-002 | The review worktree forks onto its own `review-<id-lowercase>` branch from the work item's branch, guarded by two checks (`git worktree add` itself, then `git symbolic-ref -q HEAD`), the review commit is pushed with the explicit refspec `git push origin HEAD:<branch>`, and the branch plus worktree are torn down before the round ends | This is the shape the PAGE-001 R1 reviewer converged on by improvising: it never detaches, it is explicit about where the commit lands, and the review branch is disposable. R1 of this same item found the first cut incomplete: without a check on `worktree add` itself, its failure (typically a stale `review-<id-lowercase>` left by a prior round) let the next line run against a directory that was never created and print a false "detached HEAD, stop"; and without a teardown step, that stale branch is exactly what a second round leaves behind for the third | Keeping `--detach` explicit and pushing by refspec from a detached worktree: fewer refs left behind, but the reviewer works in a state most readers find alarming, and any `git push` written without the refspec silently does nothing |
| D055 | 2026-08-04 | UI-002 | The site flips to its light-surface default: `body` background/color become cream/slate-body, and every section explicitly declares `.section--light` or a dark modifier, never left to inherit the ambient default. The footer becomes five columns with a new Legal column, reusing the existing `footer.legalEntity`/`footer.rccm`/`footer.location` keys recomposed rather than rewritten; `footer.rights` and the copyright line disappear entirely, matching `Footer.dc.html`, which has no copyright line. `RoadmapWaves`' HR deployment band moves from `--navy-alt` to `--navy-deep` to match the prototype's flat fill. The mobile nav accordion follows the header to a light background | The `.section--light` peritext (`UI-001`) works by descendant selector and only fires when the literal class sits on an ancestor; a section that only inherits the `body`'s new cream default would get a correct background with every card, badge and link still styled for a dark surface, since none of the descendant rules would match. Declaring every section explicitly, light or dark, is the only form correct regardless of Astro's per-route style-emission order, the same principle D036 already established for specificity | Flipping only the `body` default and leaving currently-ambient sections (`PageHeader`, `FeatureGrid`, `ModuleGrid`, `LegalPage`, the two hub grids, About's approach and leadership sections) unclassified; inventing a new merged dictionary key for the footer's bottom bar instead of recomposing the existing three |
| D056 | 2026-08-05 | SITE-FIX-004 | `REVIEWER.md` and `.claude/commands/review.md` again delegate the generic-correctness sweep to the `code-review` skill, invoked as `/code-review <PR-number>` with no `--effort` or `--comment` flag (the installed plugin takes neither). Its own auto-posted PR comment is accepted as a second, separate comment alongside this round's single consolidated comment, rather than suppressed | The founder installed the `code-review` plugin via `/plugin` on 2026-08-05, which removes the reason D033 gave for writing the sweep out inline. The installed skill's real interface (`gh pr diff` on the whole PR, auto-comment at its own last step) does not match what the inline text and D033 had assumed of a hypothetical `--effort`/`--comment` interface, so the wording is rewritten to the skill as it actually behaves, not merely reverted | Suppressing the skill's auto-comment by instructing the reviewer to run only its analysis steps and skip its own posting step, rejected as the founder's explicit call: two comments per round is an accepted cost, not a defect to engineer around |
| D057 | 2026-08-05 | SITE-FIX-004 | D033 is superseded: the `code-review` skill is available again, per D056 | D033's premise, that the skill is not installed and cannot be invoked from an unattended session, no longer holds once the founder installs it. D033 itself is left as written, per the standing rule that a superseded row is annotated, not rewritten | Rewriting D033 in place to say the skill is now available |
| D058 | 2026-08-05 | SITE-FIX-005 | `bin/reviewer` and `bin/autonomous_reviewer` must always be invoked via the copy living in the work item's own worktree, never via the main checkout | Both scripts `cd` to their own containing directory before reading `.claude/personalities/REVIEWER.md` and `.claude/reviewer-append.txt`. Every worktree carries its own copy of `bin/`, so invoking the main checkout's seeds the round with whatever those files say there, which drifts the moment nobody pulls it: confirmed at 20 commits behind `origin/refonte-multipages` during the R1 to R3 rounds of `SITE-FIX-002`, itself an item editing the review tooling and needing to be judged under its own in-progress version, not a stale one | Adding a freshness check or an explicit `git pull` inside the launcher scripts, which would still leave the main checkout as the default invocation path and the drift as an easy mistake to repeat |
| D059 | 2026-08-05 | UI-002 | R1's blocker and its seven important findings are fixed on the same branch rather than deferred: the footer grid widened to five tracks, `LangSwitcher`'s focus ring corrected in its own file (Astro's scoping cannot reach a child component from the parent's stylesheet), `.skip-link` gains the same border-plus-focus-colour fix as `.nav-cta` and `.btn-primary` (D039), `bin/contrast_sweep`'s success message and its silent gradient skip are corrected, the stale "dormant foundation" comment is rewritten, dead `.section--alt` and `.footer-legal-links` rules are removed, and `.lang-switcher`/`.nav-toggle` are widened to 44px on mobile | Three of the eight (the skip-link contrast, the two touch targets, and the sweep's coverage hole) are latent defects this PR only revealed by changing the surface behind them, not introduced by it; the maximal bar blocks on them regardless of origin, and fixing them here, verified, is faster and no less traceable than opening a separate item for code already touched in this diff | Opening a separate SITE-FIX item for the three latent findings and leaving PR #24 blocked on it; deferring any of the eight past this round |
| D060 | 2026-08-05 | UI-002 | R2's blocker and its seven important findings are fixed on the same branch: `.nav-toggle` keeps its 44x44 target but replaces `margin: 5px 0` on the spans with `gap: 5px` on the flex container, restoring the exact 7px centre-to-centre spacing the open-state transform is calibrated for; `bin/contrast_sweep` checks `Page.navigate`'s `errorText`, asserts at least one `.section` before accepting a page as measured, documents an `xargs`-based recipe instead of an unquoted `$PAGES` that zsh does not word-split, and rejects a path containing a newline; five tokens dead since the R1 fix commit (`--navy-alt`, `--ink-72`, `--ink-08`, `--ink-40`, `--gold-08`) are removed, along with the no-op `.card--prominent` rule R1 added; four stale or misleading comments are corrected | The blocker is R1's own fix regressing a working control, caught only by driving the mobile menu in a real viewport rather than reading the diff. The `contrast_sweep` findings matter because the tool that certifies acceptance criterion 2 was itself producing a false green on this machine's shell, the exact failure mode D048 and D055 built the tool to prevent | Reverting the 44x44 fix to the pre-R1 38x42 geometry, rejected since the touch-target finding was real; leaving the zsh recipe undocumented and hoping every operator uses bash |
| D061 | 2026-08-07 | I18N-001 | Inside the two `meta.services.description` sentences, each of the three services is named in its canonical `pageName` form, in both languages: `Conseil IT, RH & Corporate Finance` and `Développement sur mesure` in French, `IT, HR & Corporate Finance Consulting` and `Custom Development` in English. This deliberately widens `I18N-001` from the seven strings its work item lists to ten edited lines | Founder ruling of 2026-08-07, answering the one question the work item left open (`en.ts:349`, opening in lower case). The answer is that the phrase is the service name, not a description, and that the rule applies to all three services rather than to the consulting one alone. Capitalising only the service under discussion would have left one sentence naming one service as a proper noun and two as common phrases. French keeps sentence case on `Développement sur mesure` because `pageName` does, and because `Développement Sur Mesure` is not correct French typography: the rule is "the canonical name", not "a capital on every word" | Changing only the consulting name and leaving the other two in lower case, the minimal reading of the question as asked; applying an initial capital to literally every word of both sentences, rejected as it produces faulty French |
| D062 | 2026-08-07 | I18N-001 | The header dropdown absorbs the longer consulting name with no change to `Header.astro`. Measured, not read: the longest label, English `IT, HR & Corporate Finance Consulting`, has an intrinsic text width of 237px inside the 242px the 288px panel leaves it. One line, no wrap, no clipping, at 1440, 768 and a genuinely emulated 360. **The margin is 5px**, and any label longer than this one, or any change of body font, wraps it | The work item's escape hatch, "if it breaks the fix belongs to `UI-002`", expired when `UI-002` merged on 2026-08-07, so the measurement had to be conclusive rather than deferred. `Header.astro:213` also declares `min-width: 288px`, not the 250px the prototype-derived work item assumed, which is why a name the item expected to be at risk in fact fits. The 5px is recorded because the next person to lengthen a nav label will not re-measure, and this row is where they should find the number | Widening the panel pre-emptively, rejected as editing `Header.astro` from an item scoped to dictionaries; introducing a shorter navigation variant of the name, rejected as recreating exactly the three-spelling inconsistency D044 resolved |
| D063 | 2026-08-07 | I18N-001 | `footer.tagline` still carries the pre-handoff positioning in both languages and is corrected by its own item, `I18N-FIX-001`, not by this one | The two taglines are the old hero titles almost word for word, so this PR ships a page saying "Votre partenaire technologique" at the top and "Solutions technologiques" at the bottom, and an English page whose hero says "OHADA and CIMA regions" over a footer still saying "West and Central Africa", on all 26 pages. Founder ruling: the inconsistency ships and is then fixed separately, so that acceptance criterion 1 of `I18N-001` ("the seven strings change, and no other") stays checkable against the founder's rulings without a reviewer also having to judge two newly drafted sentences that no ruling covers | Correcting the taglines inside this PR, which would have meant drafting and validating new copy in the middle of an item that only applies decisions already taken; leaving the repetition as acceptable, rejected since the English pair is not merely repetitive but geographically contradictory |
| D064 | 2026-08-07 | PAGE-002 | The contact form ships **inert**: the submission markup is final (`method="POST" action="/api/contact"`), and one bundled script blocks the submit event and reveals a "form opening soon" panel. `FORM-001` deletes that script and nothing else | The submission belongs to `FORM-001`, and the endpoint does not exist yet, so a real submit would land on a 404. The inert state is never public: D049 orders `PAGE-002` before `FORM-001`, and `FORM-001` precedes the DNS cutover, so only the preview URL ever shows it | A `mailto:` fallback, which needs a published address the site does not have, hands it to scrapers, delivers a URL-encoded blob, does nothing on a machine with no mail client, and would have to be undone; a disabled submit button, which makes the one control that matters unreviewable for focus order |
| D065 | 2026-08-07 | PAGE-002 | Turnstile renders its real widget only when `PUBLIC_TURNSTILE_SITE_KEY` is set; with no key, an empty `aria-hidden` slot reserves the widget's 65px | The site key is public by design, so nothing secret is committed, and per `FORM-001`'s recorded trap no key exists on the Cloudflare project today. Reserving the exact height means the layout was verified at all three widths in the geometry it will actually have, instead of shifting the submit button when `FORM-001` sets the variable | Reserving height only and letting `FORM-001` write the widget markup, which hands it a second three-width verification, the rework this item exists to prevent; hardcoding a key, which pins a Cloudflare identifier into the repository where two environments cannot differ |
| D066 | 2026-08-07 | PAGE-002 | The three states live in the document, hidden by `hidden`, revealed from `?statut=envoye` and `?statut=erreur`. **The parameter is read client-side**, by an `is:inline` script placed immediately after the form | Carrying success in the URL rather than in a `fetch` response is what prevents the double send: when the connection drops after the POST has left, the visitor does not find an intact form and resend, and a reload redisplays the state instead of resurrecting the form. It also leaves `FORM-001` free to choose POST-redirect or `fetch`, rather than having `PAGE-002` decide its submission strategy. The reading is client-side because static output prerenders the page once and `Astro.url` carries no query string at build time; `is:inline` placed after the form runs during parsing, so the form never flashes before yielding to the success panel | A JavaScript-only toggle as the prototype does, which loses the success state exactly when the message did get through, the worst failure a commercial form has; a separate `/contact/merci` page, two more routes to declare, translate and maintain for a page meaningful only after a send |
| D067 | 2026-08-07 | PAGE-002 | A privacy sentence sits under the submit button, linking to `/confidentialite`, with no checkbox | The form collects name, company, email, phone and message, and the site already publishes a privacy policy citing Ivorian law 2013-450. The obligation to inform is met without adding friction to the only conversion point of the site | A mandatory checkbox, more defensive but one more step and one more error state to design and translate on that same conversion point; no notice at all, as the prototype has, incoherent for a company selling OHADA compliance |
| D068 | 2026-08-07 | PAGE-002 | The form's labelling diverges from the prototype on three points: the six routed select options are derived from `t.pageName` in the site's own `PRODUCT_KEYS` then `SERVICE_KEYS` order, the "(PCS)" suffix is dropped, and the two optional fields are marked "(facultatif)" / "(optional)" | A select that disagrees with the navigation is a defect, and deriving means D044's consulting rename reached the form without anyone editing it. Four fields of six are required, so marking the minority is shorter and clearer than marking the majority; the prototype marks neither, which leaves a keyboard user guessing | Copying the prototype's literal labels, which would have frozen the pre-D044 consulting name and left the select in a different order from the menu |
| D069 | 2026-08-07 | PAGE-002 | The contact-details block is deferred to a second commit on this same branch, pending an updated `contact.dc.html`. Until it lands, the error state and the no-JavaScript message promise only what is possible with no published channel | Founder ruling: the site publishes no email or phone anywhere today, and the placement of contact details is a design decision, not one to improvise in code. Building a marketing surface from a textual description alone is what the personality forbids. The gap is real and named rather than papered over: Turnstile needs JavaScript to mint a token, so without it the form cannot be sent at all, and that message can only point somewhere once a channel exists | Publishing an address chosen by the author, which the editorial guardrails forbid; shipping with no channel and calling it done, which leaves a visitor whose send keeps failing with no route at all |
| D070 | 2026-08-07 | PAGE-002 | The interest select gains an empty, `disabled`, `selected` first option carrying a prompt string, and every option's `value` becomes its stable `PageKey` (`other` for the last) rather than its translated label | Two defects with one cause, the select's value contract. Without an empty first option `required` never fires: the control is valid from the first paint and its silent default was Papillon Collection Solution, the one product labelled "Disponible T3 2026", so every enquiry sent without opening the dropdown was filed and answered as a PCS lead. That is a commercial defect on the page whose entire purpose is to qualify an inbound enquiry. And a translated label as `value` means the same interest reaches `FORM-001` as two different strings depending on the page's language, and drifts again at the next `pageName` change | Keeping `required` and accepting the default, which would have kept mislabelling leads; keeping labels as values, which pushes a grouping problem into a D1 column `FORM-001` has not written yet |
| D071 | 2026-08-07 | PAGE-002 | The colour-bearing classes of `ContactContent` are deliberately absent from the `.section--light` peritext, and that reading of D040 is recorded here rather than only in a code comment | D040 requires new colour-bearing component classes to join the peritext, but its purpose is to rescue classes born on dark-surface tokens for pages that have since gone light. These classes are born on light tokens and the component only ever renders inside a light section, so a peritext entry would restate what they already declare. `UI-001` applied the same reading to its own `.field-*` block. It belongs in the log because every other divergence in this pull request got a row, and a rule with an unwritten exception is how the next author concludes the rule was simply forgotten | Adding the classes to the peritext for form's sake, duplicating declarations; leaving the argument in a code comment only, where it reads as a private opinion rather than a decision |
| D072 | 2026-08-07 | I18N-FIX-001 | `meta.home.*` is split deliberately: the two **titles** carry the OHADA and CIMA zones, the two **descriptions** carry the founder's own new formulation, which names "Afrique de l'Ouest et Centrale" in French and "West and Central Africa" in English. FR title becomes `ALTARYS LABS \| Partenaire technologique des entreprises OHADA et CIMA`, EN title `ALTARYS LABS \| Technology partner in the OHADA and CIMA regions`. FR description is the founder's sentence verbatim, corrected for a duplicated "les" and for the missing accent on the capital of "Éditeur"; EN is its re-adaptation | Founder ruling of 2026-08-07, taken with the consequence stated in front of him: the home page now names the regulatory zones in its title and the geographic region in its description. **That asymmetry is intentional and must not be "fixed".** The zones and the region overlap heavily but are not the same territory, and the founder judged the description the right place to speak the geography a donor or an investor recognises, the title the right place to speak the regulatory perimeter that qualifies the offer. The R1 review had blocked on this pair as one defect; it is resolved as two deliberate registers | Aligning both on OHADA and CIMA, which the reviewer recommended and which the founder declined for the descriptions; naming both zone and region in the same sentence, rejected as pushing the description past 220 characters when 155 already display |
| D073 | 2026-08-07 | I18N-FIX-001 | `footer.tagline` states what the company **is** rather than repeating what the hero promises: `Éditeur de logiciels et expertise conseil, zones OHADA et CIMA.` and `Software publisher and consulting practice, OHADA and CIMA regions.` This closes D063 | The obvious correction, copying the hero into the footer, removes the contradiction and creates a duplicate: the same sentence twice per page, 26 times. A footer sits above the legal line and is where a reader looks for what the entity is, so naming the two businesses, publishing and consulting, adds information the hero and the four link columns never give. "expertise conseil" rather than "cabinet de conseil" because the site already uses that phrasing on the home page | Mirroring the hero, rejected as trading a contradiction for a repetition; a reformulated positioning line, rejected as saying the same thing as the hero in different words, which is the same waste one degree less visible |
| D074 | 2026-08-07 | D042 (annotated) | `home.heroTitle` keeps its capital in "les entreprises Africaines". The spelling is **known, deliberate and must not be corrected** by any future session or review round | French grammar puts a lower-case letter on the adjective ("les entreprises africaines", exactly as "les entreprises françaises") and reserves the capital for the noun ("les Africains", "les Français"). The rule was put to the founder with that parallel spelled out, and he chose to keep the capital as deliberate emphasis. This row exists because the string sits in the home page `h1`, the most read line of the site: without it, every competent reviewer will re-open the point, and one of them will eventually "fix" it. D042 is annotated rather than rewritten, per the standing rule | Writing `africaines` in lower case, which is what the grammar prescribes and what the R1 review recommended; leaving the capital unrecorded, which guarantees the debate is re-run |
| D075 | 2026-08-07 | I18N-FIX-001 | The residual-string defect is treated as **one family swept exhaustively**, not as the two blockers the review raised: ten dictionary values plus one stale comment, verified by a repeatable grep on the four markers (`West and Central Africa`, `African markets`, the old positioning nouns, the old service spelling). The Open Graph thumbnails and the `REVIEWER.md` contradictions leave for `SEO-FIX-001` and `SITE-FIX-007` | `I18N-001` fixed the heroes and stopped at the footer, so the R1 review found the same sentence still alive in `meta.home.*` and burnt into two PNGs. Fixing only what a review listed reproduces the defect one level further out, which is exactly how this one survived. The two follow-ups are split off because they are different materials, not lower priority: a PNG needs regenerating against the final copy, and `REVIEWER.md` is tooling whose defect (it mandates "Services first, Products second" against `CLAUDE.md`) makes reviewers raise false blockers on correct pages | Fixing only the two blockers the review raised; folding the thumbnails into this item, rejected since they must carry copy this same PR is still settling |
| D076 | 2026-08-07 | UI-003 | The site publishes contact details for the first time: `contact@altaryslabs.com` and `+225 07 20 52 22 69`. Both live in `src/i18n/config.ts` as `CONTACT_EMAIL` and `CONTACT_PHONE`, not in the dictionaries, and the `tel:` href is derived from the number by stripping spaces | They are the same string in both languages, so putting them in `fr.ts` and `en.ts` would create two copies of one fact that can drift. This is the reasoning D027 already applied to the RCCM, which is why the About page composes its legal line from `footer` rather than owning a second copy. Only the labels that introduce them are translated. Deriving the `tel:` means the dialled number cannot contradict the displayed one | Two dictionary entries per value, the obvious route, rejected on D027's precedent; a separate `phoneHref` key, which is the same drift risk one level down |
| D077 | 2026-08-07 | UI-003 | The footer's fourth column becomes Contact (email, phone, link to the form) and About moves down into Legal. `footer.columnCompany` is retired, and the Contact column is rendered outside the `{ title, keys: PageKey[] }` abstraction the other columns share | The updated `Footer.dc.html` reorganises the columns this way, and it is also the arrangement that puts a reachable address on all 26 pages rather than only on the page a visitor reaches after deciding to make contact. The abstraction genuinely does not fit: the column mixes two external protocols, `mailto:` and `tel:`, with one internal route, and forcing them into a `PageKey` list would have meant inventing fake routes | Keeping About in its own column and adding Contact as a sixth, rejected because the grid is five tracks and the handoff does not widen it; extending the `keys` abstraction with a discriminated union, more machinery than one bespoke column is worth |
| D078 | 2026-08-07 | SITE-FIX-002 | `bin/reviewer` and `bin/autonomous_reviewer` refuse to start when **the three files that seed a round** (`.claude/personalities/REVIEWER.md`, `.claude/reviewer-append.txt`, `.claude/commands/review.md`) are behind `origin/refonte-multipages`, through a single shared `bin/lib/fraicheur.sh` sourced by both rather than a copy in each. **This supersedes the alternative D058 named as rejected**, "adding a freshness check inside the launcher scripts" | Both launchers seed the round from those files as they exist in the tree they run from, with no freshness check. Round 3 of this item measured its own tree 20 commits behind and was therefore seeded with a `REVIEWER.md` predating SITE-FIX-001: it received the `--effort high` mandate D033 had retired and the instruction to compare renders against the frozen `design_handoff_altaryslabs_refonte/` folder that D037 and `CLAUDE.md` forbid, having already shipped an obsolete About page once. Same class as D052: a procedure that silently does something other than what it announces. D058's reason for rejecting the check no longer holds, because D058 itself moved the invocation into the item's worktree: the check now has a well-defined tree to measure, which it did not have while the main checkout was the default entry point. The comparison is restricted to those three paths because an item branch is behind the integration branch by construction, and stopping on any gap at all forbade reviewing every open PR of the repository, including the one delivering the guard: round 6 measured this very branch 4 commits behind and `exit=1`. **The `/review <ID>` path inside an existing session is not covered**, since no shell runs there; it stays an attended path where the founder can see what was seeded | Leaving it to operator discipline, which D052 names as the wrong answer for this class; a dedicated item, rejected by the founder since the defect was active and touched tooling the PR already edited; duplicating the guard inline in both launchers; comparing whole-tree commit counts, which is what shipped first and which blocked every round |
| D079 | 2026-08-07 | SITE-FIX-002 | The no-ID path of `/review` names steps 3, 4 and 13 as inapplicable, instead of encoding the exclusion as a step range | The path said "follow steps 5 to 12" and went stale the moment a mandatory step 13 was added, silently excluding the one step two rounds had been spent making exist. A range communicates a boundary but not a reason, so a maintainer cannot tell an exclusion from an oversight; naming the steps and saying why (this path reviews the branch already checked out, so no review worktree is opened and nothing needs tearing down) cannot go stale the same way | Bumping the range to "steps 5 to 13", which would have mandated a teardown of a worktree this path never creates; leaving the range at 12 |
| D080 | 2026-08-07 | SITE-FIX-002 | The `code-review` skill is run at `high` effort and **without `--comment`**, so it returns its findings to the reviewer and posts nothing; the pull request carries exactly one comment per round. Supersedes the premise of D056, not the founder's intent | D056 accepted two comments per round as the cost of "the skill as installed", on the stated premise that it takes no `--effort` or `--comment` flag and always posts at its own final step. Round 6 of this item is the first round that actually executed the skill, and the premise is false on both counts: it accepts an effort level and the `--comment` and `--fix` options, and it posted nothing when run without them. The founder's arbitration was made on a description of the tool that did not match the tool, so it is re-made against the real interface: one consolidated comment per round, the review file remaining the durable record. Left uncorrected, a reviewer following the text waits for a second comment that never arrives | Passing `--comment` to reproduce the two-comment behaviour D056 described, which would have preserved the letter of D056 at the cost of a duplicate the founder only ever accepted as unavoidable; leaving both documents describing an interface the tool does not have |
| D081 | 2026-08-07 | SITE-FIX-002 | The autonomous allowlist regains `Write(docs/reviews/**)` and gains `Bash(git -C:*)`, `Bash(git rebase:*)`, `Bash(bin/review_shots:*)` and `Bash(lsof:*)`. Supersedes SITE-FIX-001's claim that `Edit(docs/reviews/**)` alone grants the write | Bash rules match by command prefix, so rewriting step 13 as `git -C <START_DIR> worktree remove ...` escaped `Bash(git worktree:*)` and `Bash(git branch:*)`, which had covered the old form: removing a `cd` to avoid one missing entry created two. `Edit` requires an existing file, so a **round 1** autonomous review, where `docs/reviews/<ID>-review.md` does not exist yet, cannot write it at all; SITE-FIX-001 removed that entry as dead on a reading that only holds from round 2 onward. `git rebase` is the sole documented recovery from a non-fast-forward push and was unauthorized; `bin/review_shots` is mandated for every rendered surface and was unauthorized, making the mandatory visual check impossible in autonomous mode; `lsof` is what proves the preview port belongs to the round's own worktree. In `-p` headless mode a missing entry is not arbitrable, it is a silent failure at the last gesture | Keeping the allowlist unchanged on the ground that the `cd` had been removed, which is what this PR shipped first and which broke the teardown it exists to guarantee; widening the allowlist to `Bash(git:*)`, which would have granted `git merge`, `git reset` and `git checkout` on source and dissolved the read-only guarantee D008 rests on |
| D082 | 2026-08-07 | UI-003 | D069 is closed. The Contact page's error state and its no-JavaScript note both end with one shared sentence, `contact.fallbackText`, followed by a `mailto:` link built from `CONTACT_EMAIL` | D069 recorded that neither message could promise anything while the site published no channel, which left the page a dead end exactly for the two visitors least able to escape it: the one whose send keeps failing, and the one whose browser cannot mint a Turnstile token at all. Publishing the address in the footer removes that constraint. One sentence serves both states because they need the same thing, and duplicating it would let the two drift | A distinct sentence per state, which reads better in isolation and drifts in practice; leaving the messages as they were, now that there is a channel to name |
| D083 | 2026-08-07 | UI-003 | The 15 prototypes at the design project's ROOT are snapshotted into `docs/vitrine/refonte/prototypes/`, without `support.js` and without the frozen `design_handoff_altaryslabs_refonte/` folder. `CLAUDE.md` records that the snapshot is dated and that **the design project remains authoritative** | Versioning them makes the visual reference readable offline, diffable, and available to a reviewer who cannot reach the design project, and it makes the six known prototype errors inspectable rather than merely asserted. The authority clause is the whole point of the row: a snapshot that quietly becomes the source of truth is how someone rebuilds a page from a stale copy, which is exactly the failure the frozen handoff folder already caused once on the About page | Versioning `Footer.dc.html` alone, which leaves a half-convention where the next author looking for `contact.dc.html` concludes it does not exist; repointing `CLAUDE.md` at the local copy, which would turn a dated snapshot into the authority and reproduce the frozen-folder defect deliberately |
| D084 | 2026-08-07 | SITE-FIX-006 | The generic-correctness sweep is invoked as `code-review:code-review`, never as the bare `/code-review`. The plugin takes no options and posts its own comment, so the second comment per round is accepted again, as D056 had it. D080's `high` effort and `--comment` clauses are annotated as describing the other skill | Two skills answer to the name `code-review`. The **built-in** one takes an effort level and a `--comment` flag and stays silent without it, but carries `disable-model-invocation`: it refuses every model invocation and forbids reproducing its analysis otherwise, so only the founder can run it by typing it. The **plugin** `code-review@claude-plugins-official` carries `disable-model-invocation: false`, is invocable by an unattended round, takes no options and always posts. Rounds 3 to 5 of `SITE-FIX-002` lost the step to that collision and read it as an absent plugin; round 6 then measured the built-in skill's options and wrote them into the procedure as if they applied to the invocable one. Same confusion, two different surfaces. Verified by invoking the qualified name, which loaded the plugin and stopped at its own step 1 on a closed PR | Keeping D080's option clauses, rejected because an unattended round cannot invoke the skill they describe; requalifying the step as a founder pre-step, rejected earlier in this item as putting a manual gesture in front of every round |
| D085 | 2026-08-07 | SITE-FIX-006 | The autonomous allowlist gains `Bash(git symbolic-ref:*)` and `Edit(**/docs/reviews/**)`, and drops the inert `Write(docs/reviews/**)` | Bash rules match on a literal command prefix and file rules only on `Edit(path)`. Two mandatory gestures fell through: `git symbolic-ref -q HEAD`, which is the step 3 guard and therefore the whole substance of `SITE-FIX-002`, matched no entry at all; and the review file, which an unattended round writes inside `.claude/worktrees/review-<ID>/docs/reviews/`, matched neither `docs/reviews/**` nor a `.claude/worktrees/**/docs/reviews/**` glob. `Edit(**/docs/reviews/**)` matches at any depth while staying bounded to `docs/reviews` directories. `Write(docs/reviews/**)` is never consulted, since `Edit` rules already cover every file-editing tool, and it makes the harness warn on every launch. All four points probed against the real allowlist, before and after, each in isolation and confirmed by the file actually appearing on disk | `Edit(.claude/worktrees/**)`, rejected as authorizing the reviewer to modify source files inside a worktree, which would dissolve the structural read-only guarantee D008 rests on; adding `Bash(cd:*)` or widening to `--dangerously-skip-permissions`, both rejected for the same reason |
| D086 | 2026-08-07 | UI-004 | The ALTARYS LABS logomark is taken from the claude.ai/design project (`Header.dc.html`, `Footer.dc.html`): diamond stroked 2 for the outline, 3 for the ligature, 2.5 for the bar, `r=3` vertex dots and a 0.15 inner diamond, on the maquette's `0 0 76 70` viewBox. The wordmark leaves SVG `<text>` for real HTML text, at weight 600 for `ALTARYS` and 700 for `LABS` (see D089). `docs/vitrine/altarys-brand-identity-v3.1.html` **loses authority over the web logomark** and gains a dated amendment banner saying so; `CLAUDE.md` records the transfer | The old lockup was a single `0 0 300 72` SVG rendered at 160px in the header and 150px in the footer, so every written value was scaled by 0.53 and by 0.5: `LABS` came out 5.06px and 4.75px tall and the diamond strokes below 0.8px. Nothing in that file was the size it claimed to be, which is why the defect survived four reviews: reading the source, the values look correct. HTML text makes the written size the rendered size, and the thicker diamond is what still reads at 30px and in a 16px favicon | Re-weighting the strokes while keeping SVG `<text>`, which repairs the diamond and leaves the 5px `LABS` exactly as illegible; rebuilding the header lockup alone, impossible without a deliberate branch since both call sites share one component, and which would have put two diamond weights on the same page |
| D087 | 2026-08-07 | UI-004 | Header and footer carry **two distinct lockups from one component**, through `lockup` (`stacked` / `inline`) and `detail` (`full` / `simple`). The header stacks ALTARYS over LABS and keeps the vertex dots and inner diamond; the footer sets them on one line and strips both. Sizes track the lockup, colours track the surface | Each comes from its own prototype and each was validated separately by the founder on 2026-08-07; the footer's stripped mark is an intention, not a prototyping residue, and was confirmed as such. Splitting the two axes means a surface change never has to be mirrored by hand into a size rule, and the diamond geometry exists once | One shared lockup everywhere, simpler to maintain but silently discarding the footer prototype; two separate components, which duplicates the diamond and guarantees the next geometry change lands in only one of them |
| D088 | 2026-08-07 | UI-004 | `Logo.astro` drops `width` and `title`. The accessible name moves onto the calling anchor's `aria-label`, which `Footer.astro` already did and `Header.astro` now does too, and the SVG is permanently `aria-hidden` | `width` drove a viewBox that no longer exists, so keeping it would have meant honouring a scale the component cannot apply. With the wordmark as real text, an SVG `<title>` would give one link two accessible names, read one after the other. No `Dictionary`-style build guard covers props, so `astro check` is the only net here; both call sites move in the same commit and it is green | Keeping `title` as an optional override, which leaves the doubling trap armed for whoever adds the third call site; keeping `width` as a no-op for compatibility, which lies to the next reader about what the component can do |
| D089 | 2026-08-07 | UI-004 | `.logo-altarys` is set to **`font-weight: 600`**, not the 700 both prototypes write. `LABS` keeps 700. Raised by review round 1 | `BaseLayout` loads `DM+Sans:wght@300;400;500;600`, with no 700, and the prototypes carry that identical `<link>`. A 700 therefore already paints with the 600 face, without even synthetic bolding: the advance width of `ALTARYS` measured identical to the thousandth at both weights. Writing 600 changes nothing on screen and matches what the founder approved on the preview; writing 700 left a declaration nothing could satisfy, which would have silently re-weighted the logo on all 26 pages the day someone appended `;700` to the font link without opening this file. `LABS` is unaffected because Space Mono does load 700 | Adding `;700` to the font link, rejected because it would make the logo heavier than the rendering the founder validated, and costs a further font file for one word; leaving 700 in place as aspirational, which is the trap described above |
| D090 | 2026-08-07 | FORM-001 | Storage is confined to **`server/contact-store.ts`**, the only file in the repository containing SQL or naming D1. `functions/api/contact.ts` orchestrates and calls `storeContactRequest`. The three shared modules live under `server/` at the repository root, **not inside `functions/`**, and **no dependency is added**: the D1 surface actually used is hand-declared in the store | D1 is provisional, with relocation to Africa targeted for late 2026, so the provider must be swappable by editing one file. Verified by `grep -rln -e "INSERT INTO" -e ".prepare(" functions server src`, which returns exactly one path. Modules sit outside `functions/` because the Cloudflare docs reachable this session did not confirm that underscore-prefixed paths are excluded from routing, and a routing surprise there would publish an internal module as an endpoint; an import escaping the directory is bundled by esbuild unambiguously, which `npx wrangler pages functions build` confirms. `@cloudflare/workers-types` was refused because it redeclares `Request`, `Response` and `Headers` that Astro's DOM lib already provides, and the collision fails `npm run check` project-wide; hand-declaring also makes the dependency surface the exhaustive list of what a replacement must offer | `functions/api/_lib/*.ts` relying on the unconfirmed underscore rule; adding `@cloudflare/workers-types`; letting the handler hold the SQL, which is what the work item was written to prevent |
| D091 | 2026-08-07 | FORM-001 | The submitting page's language travels in a **hidden `locale` field in the POST body**, never inferred from the `Referer` header. The field is a two-value enum, never a URL: the handler validates it with `isLocale` and reads the target from `ROUTES`, so a tampered value can only ever produce one of the site's two contact pages | Both pages post to a single `/api/contact`, so the function must be told where to send the visitor back. `Referer` is optional by design and is stripped by privacy extensions, corporate proxies and `Referrer-Policy: no-referrer`; the resulting failure is silent, absent from every log, and unreproducible in the author's own browser, because a developer's browser always sends the header. Deciding factor: the language is **data**, not routing metadata. "This person wrote from the French page" is a fact the sales team acts on and D093 stores. Derived from `Referer` it would be stored as a guess that silently defaults to French, so a later count of anglophone enquiries would be wrong with nothing in the table saying so. Prior art agrees: Django, Laravel and OAuth all carry the return target in an explicit parameter, and Rails' `redirect_back`, the one API that does use `Referer`, makes `fallback_location` mandatory | The `Referer` header, for the reasons above; two endpoints (`/api/contact` and `/api/contact-en`), which contradicts the final `action`, costs a markup change anyway and breaks the property that adding a language is a `routes.ts` table edit; `action="/api/contact?lang=fr"`, which carries A's constraints with none of its benefits since the query string describes the resource while the language describes the submission |
| D092 | 2026-08-07 | FORM-001 | Native POST then **HTTP 303**, never `fetch`. The target is `ROUTES.contact[locale]` plus `?statut=envoye` or `?statut=erreur`, resolved against `request.url` so preview and production are both correct, with `Cache-Control: no-store`. A **GET** on the endpoint answers 303 to the French contact page with `X-Robots-Tag: noindex` | This is the choice D066 left open, and its rationale settles it: carrying success in the URL means a connection dropping after the POST has left does not hand the visitor an intact form to resend. Reading the target from `ROUTES` keeps routing single-sourced, so no bilingual URL is hardcoded in the function. The GET handler was added after local testing showed `wrangler pages dev` serving the **homepage at 200** on `/api/contact`, which is duplicate content for a crawler and a dead end for a human following a stray link; a bare 405 would be protocol-correct and just as much of a dead end | `fetch` plus a client-side status swap, which reintroduces the double-send D066 exists to prevent; a 302, whose method-rewriting is not guaranteed; a 405 on GET; leaving the GET unhandled, which is the observed 200 defect |
| D093 | 2026-08-07 | FORM-001 | The table stores **exactly the six fields section 2 of `/confidentialite` enumerates**, plus a server-set ISO 8601 timestamp and the page language. **Never the IP address, the user-agent or `CF-IPCountry`.** The optional fields are stored `NULL`, not empty string, and `remoteip` is deliberately not passed to Turnstile's `siteverify` | The published privacy policy closes its list with "Aucune autre donnee n'est collectee a votre insu sur ce site". Storing any of the three would put the code in contradiction with a text already live, which is a legal exposure and not a technical detail. Not passing `remoteip` means the visitor's address is never read at any point of the chain, so the conformity is structural rather than a rule someone has to remember. `NULL` versus empty string matters because a column that cannot distinguish "not provided" from "provided empty" turns any later segmentation into a filter on a magic string. Sections 6 and 7 of the policy already name Cloudflare, Europe and Resend correctly, so no legal text needed editing | Storing the country for analytics; storing the user-agent for debugging; passing `remoteip` to `siteverify` for marginally better anti-fraud; using empty strings for the optional fields |
| D094 | 2026-08-07 | FORM-001 | **SUPERSEDED THE SAME DAY BY D100**, which removes the variable altogether so the key can no longer be absent. The row stands as written because the guard did its job: it turned a silent, pre-existing platform misconfiguration into a red build within the hour. Original decision follows. **`ContactContent.astro` throws at build time when `PUBLIC_TURNSTILE_SITE_KEY` is absent**, and D065's empty-slot fallback with its two ternaries is deleted. **D065 is closed** | The variable is read by Astro at compile time and frozen into the static HTML. D065's fallback was correct while nothing was actually sent: it let the layout be verified before the key existed. Now that submission works, a build without the key would ship a form that looks perfect to a reviewer and whose every submission is refused server side for lack of a token. That defect is invisible in review and fatal to the visitor, so the only safe behaviour is to fail loudly, which the wrangler comment and D065 itself already assigned to this item. Verified both ways: the build fails with an actionable three-line message naming the dashboard path, and succeeds once the key is present | Warning at build and keeping the fallback, which is a line in a CI log nobody reads; deferring the point to a later item, which leaves open a regression D065 explicitly handed to FORM-001 |
| D095 | 2026-08-07 | FORM-001 | The D064 stopgap is removed **in full**: the submit-blocking script, the `#form-pending` panel, and `pendingTitle` and `pendingText` in both dictionaries. **D064 is closed** | D064's own wording says "deletes that script and nothing else", but the `fr.ts` comment on those keys is explicit that FORM-001 "supprimera ce panneau et le script qui le declenche". Removing the script while leaving an unreachable panel and two dead keys would leave the next reader unable to tell whether the panel is a bug or a pending feature. Founder confirmed the wider removal at the plan gate | Deleting the script alone and leaving the panel and keys as dead code; keeping the keys "in case", which is how a dictionary accumulates strings nobody dares delete |
| D096 | 2026-08-07 | FORM-001 | The Resend sender is a **constant in `server/notify-resend.ts`**, not a fifth environment variable, with `reply_to` set to the visitor's address. The notification email is written **in French**, and carries the visitor's language in the body. The interest is rendered through `fr.pageName`, not as the raw key | The `From` address is public by nature since it travels in every message header, so a variable would add maintenance on two environments and buy nothing. The email is internal, going to `CONTACT_NOTIFY_EMAIL` and never read by the visitor, so French is the repository's working language applied consistently; the visitor's language is stated in the body because it dictates the language of the reply. `reply_to` lets the sales team answer without copying an address by hand. Labelling through `pageName` means a page rename reaches the notification without anyone thinking about it, exactly as D068 argued for the select, and the resolver is total so an unexpected value renders as itself instead of printing `undefined` in an email | A `CONTACT_FROM_EMAIL` variable; sending the notification in the visitor's language, which would make an internal inbox bilingual for no benefit; emailing the raw `PageKey`, which makes the sales team decode `servicesConsulting` |
| D097 | 2026-08-07 | FORM-001 | Order is Turnstile, then validation, then the D1 write, then the notification. **A failed notification does not fail the request**: the visitor sees success and the failure is logged. A failed write does show the error state | Once the row is in the database the enquiry is not lost, so telling the visitor to resend would only produce a duplicate for the sales team to reconcile. The log is the recovery channel for the missing notification. The converse is not symmetric: if nothing was stored, the error state is the only honest answer. Turnstile runs first so a robot consumes neither a write nor an email. Server-side validation rejects to the generic error state without a dedicated message, because the browser already blocks that case through `required` and `type="email"`, so a request reaching it was not posted by the site's form | Treating any failure as an error, which loses stored enquiries to a duplicate; sending the email before the write, which can notify a request that was never persisted; writing a distinct message per validation failure, which serves only robots |
| D098 | 2026-08-07 | FORM-001 | `.gitignore` gains `.env`, `.env.*`, `.dev.vars` and `.dev.vars.*`, with `!.env.example` kept open | The file listed build outputs and editor directories but nothing that would stop a local secrets file being committed, while this item introduces the first four secrets the project has. Acceptance criterion 4 is a hard blocker, and a hard blocker enforced only by the author's attention is not enforced. `.dev.vars` is included because it is wrangler's own local secrets file and would otherwise be the likeliest accident | Relying on review to catch a committed `.env`; adding the patterns only when someone first needs a local secrets file, which is exactly the session where the mistake happens |
| D099 | 2026-08-07 | FORM-001 | `wrangler.jsonc` `name` is corrected from `altaryslabs-com` to **`altaryslabscom`**, the real Pages project name, with the project's subdomain and production branch recorded in a comment beside it | Confirmed against the Cloudflare API rather than inferred: the account holds one project named `altaryslabscom`, subdomain `altaryslabscom.pages.dev`, production branch `refonte-multipages`. The Git-driven build ignores this field, so nothing was broken today, and that is precisely why the drift survived. Every hand-run wrangler command does follow it, and a `wrangler pages deploy` against a name that does not exist **creates a second project** instead of targeting this one. That is the 31 July 2026 incident already recorded in `FORM-001`, whose duplicate was deleted without anyone noting what it held, which is why this item had to assume no secret existed on the project. Fixing the name removes the cause rather than the symptom. A second consequence worth knowing: because the production branch is `refonte-multipages` and not `main`, merging into the integration branch publishes to `altaryslabscom.pages.dev` directly, so end-to-end verification happens on a stable URL and not on a per-commit hash | Leaving the drift because the Git build ignores the field, which is how it survived this long; deferring to a separate item, rejected because the file was already open and the trap has cost a project once |
| D100 | 2026-08-07 | FORM-001 | **The Turnstile site key becomes a constant in `src/i18n/config.ts`**, `TURNSTILE_SITE_KEY`. `PUBLIC_TURNSTILE_SITE_KEY` is retired: the project now has **no build-time variable at all**, only three runtime ones. The empty-slot fallback and the build-fail guard both disappear, since the key can no longer be absent. **Supersedes D094 and finishes closing D065** | Found by the first Cloudflare build on PR #34, which failed while the dashboard showed the variable set in Production and Preview. The log is the whole story: `Found wrangler.json file. Reading build configuration...` then `Build environment variables: (none found)`. Because `wrangler.jsonc` carries `pages_build_output_dir`, that file is the source of truth for project configuration and Cloudflare stops reading dashboard variables for the build. `pages_build_output_dir` predates `FORM-001`, so dashboard build variables had **never** reached this build; nothing depended on one until now, because D065's fallback silently absorbed the absence. D094's guard did not cause the failure, it revealed a latent condition, which is exactly what it was written for. The decisive argument for a constant rather than fixing the plumbing: the value is **already public and must be**, since Turnstile requires the site key in the rendered HTML, and rotating it **already required a rebuild** because it is frozen into static HTML at compile time. Moving it into code therefore costs zero operational flexibility while making the failure impossible, and keeps production configuration in version control. Verified by building with `env -u PUBLIC_TURNSTILE_SITE_KEY`, which now succeeds and emits the key on both contact pages | A `vars` block in `wrangler.jsonc`, rejected because the docs describe `vars` as a Function runtime binding rather than a build variable, so it risked a second red build while committing the same value anyway; removing `pages_build_output_dir` to hand configuration back to the dashboard, rejected because the D1 binding and build output would then be hand-entered settings that no file records, which is the same failure mode as the 31 July duplicate project; keeping the build variable and hunting the plumbing, which fights the platform for a value that is public by design |
| D101 | 2026-08-07 | UI-FIX-003 | The two share images are **generated by `bin/og_images`**, not hand-drawn: headless Chrome renders a 1200x630 template from the real Google fonts and the D086 logomark, and **nothing that can drift is written in the script**: the copy comes from `fr.ts` and `en.ts`, title, emphasis and subtitle alike, and the diamond geometry from `public/assets/favicon.svg`. The generator refuses to write when a font failed to load | They were opaque rasters nobody knew how to redo, which is why UI-004 shipped a site logo they contradicted, and why the English one still carried "Enterprise software built for African markets", a positioning `en.ts` had already retired, on every share of all 26 pages. The French one was equally stale and no review had caught that one. Reading every varying value from its own source closes the class rather than one instance of it: a share preview can no longer say something the home page does not, nor paint a logomark the site has stopped using. Review round 1 of this item is why the claim is now true and was not when first written: the subtitle and the diamond were still transcribed into the template, so the row asserted a guarantee two hardcoded values contradicted in the same commit. The load guard matters for the same reason: `document.fonts.status` reads 'loaded' whether the faces arrived or not, so the first version would have overwritten both PNGs with a Times rendering and exited zero | Regenerating by hand in a design tool, which reproduces the original defect the day the logo next moves; keeping UI-005 as a tracker, rejected because review round 2 established that tracking a blocker does not discharge it |
| D102 | 2026-08-07 | UI-FIX-003 | `docs/vitrine/altarys-brand-identity-v3.1.html` is declared **obsolete in full**, not merely on the logomark. Supersedes the narrower clause of D086 | D086's amendment banner said the document "remained the reference for colours". Its colour section lists `#D4810A`, `#F5A623`, `#1A8FA0` and `#4DB8CC`, the amber and teal palette D015 removed, under a heading presenting ALTARYS ENTERPRISE as a live product line although D017 replaced it. The banner therefore pointed the next reader at values `CLAUDE.md` forbids, which is worse than no banner at all: it lent authority to the exact pages that would mislead. Founder's call, taken on the review finding | Narrowing the banner to typographic scales only, which keeps a half-valid document that must be read carefully to be used safely; purging the amber and teal swatches from the file, rejected because it rewrites a dated archive |
| D103 | 2026-08-07 | FORM-FIX-001 | The Turnstile widget carries **`data-language={locale}`**. Raised as a blocker by `FORM-001` review round 1 | The attribute defaults to `auto`, which follows the browser's `Accept-Language` rather than the page's `lang`. `/en/contact` therefore rendered the widget's **"Verification..."** in French. Two failures at once: a French string inside an English page, which the parity rule treats as critical, and a French fragment with no `lang` of its own, a WCAG 3.1.2 Language of Parts failure. The exposure is the common case rather than an edge: this market's browsers are overwhelmingly French-configured, so the English contact page showed a French anti-robot control to most of the audience it exists for, on the one page where a prospect is about to hand over their name. The author's own parity checks looked at the dictionaries and at the HTML this repository emits, never at HTML injected by a third party at runtime, which is the gap the review closed | Leaving it to `auto` and treating third-party chrome as out of scope for the parity rule, which would ship a French control on every English page |
| D104 | 2026-08-07 | FORM-FIX-001 | **The submitted values survive the error redirect**, persisted to `sessionStorage` on submit and rehydrated when `?statut=erreur` is read. The draft is cleared on success and kept after rehydration. **Amends D092** | D092 justified POST-then-redirect by the double send it prevents on success, and that reasoning stands; it never examined the mirror cost on the error side, and D097 called the error state "the only honest answer" without noticing the honest answer arrived with the message deleted. The 303 lands on a static page, so the visitor met a blank form under a panel telling them to try again, an instruction the page had made impossible to follow without retyping everything. For a director who has just written several paragraphs about a 20 to 100 million FCFA tender, one refused token cost the whole enquiry, and the likely outcome is that they do not retype it. `sessionStorage` rather than echoing values back in the redirect: a query string carrying the visitor's message would be logged at every hop. Only the six named fields are stored, which excludes the Turnstile token by construction, and that exclusion is necessary since the token is single use and restoring it would guarantee a refusal. The draft is kept rather than cleared after rehydration so that reloading the error page keeps the text; it dies with the tab regardless | Echoing the values in the URL, heavier and leaking content into logs; clearing on rehydration, which loses the text on a reload; doing nothing, which is the defect |
| D105 | 2026-08-07 | FORM-FIX-001 | Both state panels gain **`tabindex="-1"`**, and focus moves to the revealed one on `DOMContentLoaded`, with the reduced-motion-aware scroll | `role="alert"` on the error panel was inert: the inline script reveals it during parse, and a live region present at first render is part of the initial page state rather than a change to it, so assistive technology has nothing to announce. Moving focus does get it read. Neither panel could be focused at all without `tabindex="-1"`, so a `focus()` call alone would have been a silent no-op. This is a regression the branch created: the D064 stopgap that `FORM-001` deleted carried `tabindex="-1"` and called `focus()`, and `PAGE-002` review round 1 had explicitly deferred the request to `FORM-001`, which then shipped the real error path without it. Focus is deferred to `DOMContentLoaded` because a `focus()` issued mid-parse is unreliable, while the reveal stays in the parse phase so nothing flashes | Keeping `role="alert"` alone, which announces nothing; focusing during parse, which is unreliable; dropping the announcement as a minor concern, when the panel is the entire feedback of the site's only conversion point |
| D106 | 2026-08-07 | FORM-FIX-001 | **`CONTACT_NOTIFY_EMAIL` becomes an optional override**, not a dependency: absent, the notification goes to `CONTACT_EMAIL` from `config.ts`. And **each failure cause gets its own log line** instead of one message naming two variables | The variable had never been set, and three real submissions were stored with no notification sent. Nothing was lost, because D097 stores before notifying, but the founder only learned of it by watching: in normal operation a prospect would have written and no one would ever have known, on the only conversion point of the site. Same class of error as D100's site key, and the same platform trap explains it: a plain-text variable does not reach a project configured by `wrangler.jsonc`, so it could not even be created as text in the dashboard. A value that is not secret and already lives in the repository must not be turned into a platform dependency that can vanish silently. The log split matters as much: "RESEND_API_KEY ou CONTACT_NOTIFY_EMAIL absente" sent the investigation after the Resend key, which was correct all along, and a message that offers two causes does half the work | Setting the variable and moving on, which leaves the failure mode intact for the next environment; making the recipient a second constant with no override, which loses the ability to route notifications away from the public address |
| D107 | 2026-08-07 | FORM-FIX-001 | The `hostname` reported by `siteverify` is **compared to the request's own host**, and a mismatch is refused. Not an allowlist | A valid token was not enough. The site key is public by design and sits in the served HTML, so anyone could serve a page on any hostname the widget allows, solve the challenge, and post the token to production; with `localhost` on the widget's list this was doable from a desktop, which hollowed out the whole of acceptance criterion 2. Comparing against the request host rather than a list is the better shape: the form posts to `/api/contact`, a relative URL, so page host and POST host are always identical for a legitimate submission. Nothing to maintain, nothing to update at the DNS cutover, and nothing that breaks silently on a per-branch preview URL, whereas a list would have had to enumerate `altaryslabs.com`, its `www`, the `pages.dev` subdomain and every preview subdomain, and would have failed on the first one nobody thought of. Verified locally: Cloudflare's test secret always reports `example.com`, so a token minted under it is refused against a `localhost` request, with both values named in the log | An explicit hostname allowlist, as review round 1 suggested, rejected for the maintenance burden and the silent failure at cutover; relying only on removing `localhost` from the widget, which fixes today's instance and not the class |
| D108 | 2026-08-07 | SITE-FIX-010 | `docs/AI_Development_Workflow.md` gains a **Follow-up items born from a review** section carrying two rules: a follow-up item's scope must **quote the reviewer's defining sentence verbatim** rather than paraphrase it, and findings must be **classified by the severity the reviewer assigned**, never by how cheap the fix looks | Both were broken on `FORM-001` round 1, in the same plan. The review named one follow-up item and scoped it to "the blocker and the important findings"; the plan proposed a two-item split, deferred three `IMPORTANT` findings to the second item, and described that split as what the review required. The split itself was arguable; presenting it as an external requirement was not, because it left the founder having to argue with a document instead of with the author. The second rule names the mechanism that produced the first: the four remaining findings were grouped by effort, all being one-line corrections, and the group was then labelled after its most trivial member, which is how `IMPORTANT` findings landed in a `CHR` item. **The rule is mechanical on purpose.** A plan gate cannot detect a mislabelled premise: the founder is reading the author's framing, so no amount of care at the gate substitutes for quoting the source. Found by the founder reading the proposed prompt, not by any check the workflow had | Trusting the author to represent a review faithfully, which is exactly what failed; requiring the whole review to be pasted into every plan, rejected as unreadable when only the scoping sentence is load-bearing |
| D109 | 2026-08-07 | SITE-FIX-010 | The decision-log section states that **a D-number can be born after the survey**, so the highest number must be **re-checked immediately before committing** and not only at plan time | The existing instruction to grep every open branch was correct and insufficient, and the gap is not obvious: it reads as a completeness rule about branches when the real hazard is timing. A review round routinely adds D-rows to a branch that is already open, so a number free when the plan was written can be taken before the work is committed. `FORM-FIX-001` surveyed every branch, found `UI-FIX-003` holding D101 alone, reserved D102 to D106, and collided because that item's own review round then added D102. The collision surfaced as a merge conflict and cost a renumbering of five rows across four code files and a work item doc. Recorded the same day as D108 because both come from the same session and both replace vigilance with a mechanical step | Leaving the rule as it stood, which had already failed once; renumbering at merge time as normal practice, which makes every D-row reference in code comments provisional |
| D110 | 2026-08-07 | FORM-FIX-001 | The contact form's environment variables are **enumerated in exactly one place**, the `Env` interface of `functions/api/contact.ts`, which names each one, marks it required or optional and states the behaviour in its absence. **The rule binds the three files that speak for this repository**: `CLAUDE.md`, `wrangler.jsonc` and `.gitignore` carry **neither the list nor its count** and point at the interface instead. `wrangler.jsonc` keeps only what belongs to it alone, the dashboard procedure, the Secret-versus-Text rule and the inert-plain-text trap. **`docs/kb/turnstile-d1-resend-setup.md` is deliberately outside the rule**: it is a reusable tutorial for setting up Turnstile, D1 and Resend on any Astro project, not a description of this project, so a step-by-step list of what to type is its subject rather than a copy that can rot. It says so in its own words and points at the interface for the list of record | Six copies of a fact that moves. The same defect landed twice in a row: D100 removed a variable and left `.gitignore` saying "quatre variables", D106 turned one from required into optional and left `CLAUDE.md`, `wrangler.jsonc` and the header of `server/notify-resend.ts` describing the opposite of the code. Both times the trigger was a **change of behaviour**, which is why the description has to sit next to the behaviour rather than in the document that reads best. The count is what rots fastest and buys the least, so no binding copy is allowed to carry one. The failure is expensive rather than cosmetic: `CLAUDE.md` is what the next session debugging this form reads before the code, and a plain-text variable that cannot reach this project already cost three real contact requests that were stored and never notified. **The runbook exclusion is a correction, not an original clause**: the row first claimed the rule covered that file too, review round 2 verified line by line that it still carried "Les deux cles" above a two-row table, and the founder ruled `docs/kb/` out of this item's reach. A rule asserted over a file it does not hold is worse than a narrower rule, because the next reader trusts it | Making `CLAUDE.md` canonical, rejected because it is the furthest document from the code that changes, which is the cause of both drifts; correcting the three places round 1 named and keeping six descriptions in hand-sync, rejected because that is precisely the process that failed twice; deferring the consolidation to a separate item, rejected because the files were already open and the third drift would have landed first; extending the rule to the runbook, rejected because a portable tutorial that refuses to list what to type stops being usable | Six copies of a fact that moves. The same defect landed twice in a row: D100 removed a variable and left `.gitignore` saying "quatre variables", D106 turned one from required into optional and left `CLAUDE.md`, `wrangler.jsonc` and the header of `server/notify-resend.ts` describing the opposite of the code. Both times the trigger was a **change of behaviour**, which is why the description has to sit next to the behaviour rather than in the document that reads best. The count is what rots fastest and buys the least, so no copy is allowed to carry one. The failure is expensive rather than cosmetic: `CLAUDE.md` is what the next session debugging this form reads before the code, and a plain-text variable that cannot reach this project already cost three real contact requests that were stored and never notified | Making `CLAUDE.md` canonical, rejected because it is the furthest document from the code that changes, which is the cause of both drifts; correcting the three places R2 named and keeping six descriptions in hand-sync, rejected because that is precisely the process that failed twice; deferring the consolidation to a separate item, rejected because the files were already open and the third drift would have landed first |
| D111 | 2026-08-07 | FORM-FIX-001 | The `sessionStorage` draft introduced by D104 is **not a collection** within the meaning of the privacy policy, and **no line is added** to `/confidentialite` or `/en/privacy`. Founder's decision, recorded so the question is not reopened each round | The visitor's input never leaves their own browser, dies with the tab and is never transmitted, so section 2's closed list of collected data stays exact and section 9 stays true. The review round that followed `FORM-001` round 1 raised it as a suggestion while confirming nothing is false today. Recording the reasoning rather than the silence is what makes the next round cheap: an unrecorded non-decision looks identical to an oversight, and this one sits on a legal page the repository has deliberately locked | Adding a sentence to section 9 in both languages, rejected because it opens a locked legal text inside a documentary-corrections PR to describe something the policy does not govern; leaving the question to a later item, rejected because it would be re-raised at every round until answered |
| D112 | 2026-08-07 | FORM-FIX-001 | **A second submission sent before the redirect is cancelled**, by a flag and `preventDefault` on that second submission alone, with the submit button disabled afterwards as visual feedback and never as the mechanism. A `pageshow` listener re-arms both on a `persisted` restore | The Turnstile token is single use, so two POSTs fired before navigation, which one double click produces, give a first submission that succeeds and a second that Cloudflare refuses as `timeout-or-duplicate`. The row is written, the email is sent, and the visitor reads an error panel: the page states the opposite of what happened, on the site's only conversion point, to someone who has just spent minutes writing about a tender. The window is short and the severity low, but a false message costs more than a slow one. **Cancelling the second rather than disabling the button** is what keeps the first submission untouched, which is the hard constraint: a button disabled inside its own submit handler is a mechanism whose behaviour varies, while a flag read at the top of the handler is unambiguous. The `pageshow` guard closes the trap the fix itself opens: the back button can restore the page from the bfcache with the button still disabled, leaving a form that can no longer be sent, which is worse than the defect being corrected. The error return needs nothing, being a fresh navigation whose static markup never carries `disabled`. **Not the duplicate-submission detection** `FORM-001` puts out of scope: nothing is de-duplicated server side, the page merely stops claiming a failure that did not happen | Disabling the button alone, rejected because it makes the protection depend on browser behaviour inside the event that must not be disturbed; an idempotency key server side, which is the real duplicate detection and a far larger item; doing nothing, given the short window, rejected because the visitor is told their request failed when it succeeded and the likely reaction is to send it again or to give up |
| D113 | 2026-08-07 | FORM-FIX-001 | **`SITE-FIX-012` is opened** to carry what this item defers: the `docs/kb/` and review-corpus language frontier, the incomplete rule D106 left in D100, the three non-blocking suggestions of `FORM-001` round 1, and the two latent defects the last round confirmed, the French colon spacing in three English `aria-label`s and the unused off-palette `--ok` and `--err` tokens | The repository rule is that an out-of-scope defect materialises as a work item **plus** a D-row, and that the follow-up adds to the block rather than replacing it. This item broke that rule twice: `FORM-CHR-001` was named in a work item and a PR body while existing nowhere, and its replacement was deferred to a `SITE-FIX-011` that had no fiche, no branch and no D-row either. Both were abandonments wearing the costume of a follow-up, and both were caught by review. `SITE-FIX-011` is held by another session, hence `012`. The two latent defects are grouped here on the founder's call rather than fixed in place, because they change rendering on every `/en/` page and on the CSS of every page, which would take a documentary PR out of its own scope and force a full visual pass | Fixing the two latent defects inside this PR, rejected for the scope widening; leaving the five deferred points unowned, which is the abandonment the reviews blocked twice |
| D114 | 2026-08-07 | FORM-FIX-001 | **`SITE-FIX-013` is opened** for the structural cause behind the lost `FORM-001` review: step 11 of `.claude/commands/review.md` pushes the round's record onto the work item's branch, and the same file forbids committing one on `refonte-multipages`, so any round that runs **after** its PR is merged writes its record onto a dead branch and it never reaches the integration branch | The instance was repaired by cherry-picking `62a2148`, but the cause is a procedure rather than an accident, and it destroys the only durable record a round leaves behind. It has already fired once, silently: `docs/reviews/FORM-001-review.md` existed solely on `feat/formulaire-contact` and the PR link 404ed for anyone who followed it. The repository treats the review file as the sole durable record of a round, so losing one empties the review loop of its memory, and nothing in the current procedure warns the reviewer. Naming the item is itself the point: the previous round's deferral of this same cause carried **no identifier at all**, which the reviewer correctly called the degree zero of follow-up, since nothing can retrieve it | Repairing the instance and saying nothing, which is what leaves the next round to lose its own record; folding the procedure change into this PR, rejected because it edits the reviewer's own instructions from inside an item the reviewer is judging |

D033 to D035 are reserved by `SITE-FIX-001` on branch `fix/outillage-reviewer`,
not yet merged. The gap is deliberate, not a missing entry.

D108 and D109 belonged to `SITE-FIX-010` and **landed on the integration branch
through PR #37** while `FORM-FIX-001` was still open, which is why D110 to D114
were written above a gap. There is no gap left: this note is kept because a
reader comparing the two items' history will find the numbers interleaved, and
because the survey rule that produced the gap is worth stating once. **A
D-number can be reserved before the work that fills it is committed**, so a
survey of the repository's references is a lower bound and never a proof that a
number is free. See D109, which states the same thing from the other side.
---

Rows D011 to D022 are back-filled. They record decisions that were locked
during PR #1 to PR #12, before this log existed. Their dates are the dates each
decision actually took effect, not the date it was written down, and the Item
column carries the pull request, since the work item nomenclature was only
introduced by PR #3.
