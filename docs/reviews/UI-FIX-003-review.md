# UI-FIX-003 - Review

## Round 1 - 2026-08-07
**Verdict**: CHANGES REQUESTED

Base diffed: `origin/refonte-multipages...origin/fix/vignettes-og`. `npm ci`, `npm run build` and `npm run check` run independently in a dedicated review worktree: build green, 26 pages, `astro check` 0 errors / 0 warnings / 0 hints. `npm run preview` served on **port 4322**, not 4321, which was held by an unrelated project; captures taken against 4322 after confirming the listener's `cwd` is this review worktree.

The two share images were opened directly, and `bin/og_images --check` was run twice: both report `inchangee` on both files. The generator is deterministic and reproduces the committed bytes exactly. That part of the item is genuinely delivered, and the images are correct: D086 diamond, hero copy matching `fr.ts` and `en.ts`, 1200x630, 66.3 and 66.4 Ko. The FR and EN home pages were captured at 360, 768 and 1440 px in both languages and show no regression.

The findings below are about what surrounds that result.

### Blockers

- [ ] **[BLOCKER]** `docs/DECISIONS.md:109` - **D100 is already taken.** `feat/formulaire-contact` merged as PR #34 at 20:44 today, before this PR was opened, and it carries **D090 to D100**, not D090 to D099. Its D100 is the Turnstile site key constant. The reservation note at `docs/work-items/UI-FIX-003.md:66-67` miscounts the range by one, and the branch it calls "open" is merged. Verified two ways: `git show origin/refonte-multipages:docs/DECISIONS.md` lists D100 for FORM-001, and `git merge-tree --write-tree origin/refonte-multipages origin/fix/vignettes-og` conflicts on `docs/DECISIONS.md`. GitHub agrees, `mergeable: CONFLICTING`. Two contradictory D100 rows would silently corrupt every later citation of that number -> rebase on the current `refonte-multipages` tip, renumber to **D101 and D102**, rewrite the reservation note, and update all nine citation sites: `docs/DECISIONS.md:109-110`, `CLAUDE.md:67,80,91`, `bin/og_images:18`, `docs/work-items/UI-005.md:15`, `docs/work-items/UI-FIX-003.md:6,45,67`, `docs/vitrine/altarys-brand-identity-v3.1.html:187,189`.

- [ ] **[BLOCKER]** `bin/og_images:255-265` - **the font guard does not detect a font-loading failure**, so a run without network silently overwrites both share images with a fallback-font rendering. `document.fonts.status` is `'loaded'` once loading has *finished*, whether the faces succeeded or failed; when the stylesheet itself never arrives, `document.fonts` is simply empty and the check passes immediately. Verified empirically: a copy of the script with the Google Fonts host pointed at an unresolvable domain rendered Times and Helvetica instead of Cormorant Garamond, DM Sans and Space Mono, overflowed the title to three lines, pushed the gold rule off the canvas, wrote both PNGs and **exited 0**. The comment at lines 250 to 254 and the abort message at line 261 both claim a protection the code does not provide, and this is precisely the "share image nobody notices is wrong" failure the item exists to close -> assert the families actually resolved after `document.fonts.ready`, for instance `document.fonts.check('300 63px "Cormorant Garamond"') && document.fonts.check('600 31px "DM Sans"') && document.fonts.check('700 12px "Space Mono"')`, and abort if any is false.

- [ ] **[BLOCKER]** `.claude/personalities/TECH_LEAD.md:22`, `.claude/personalities/REVIEWER.md:70`, `.claude/commands/review.md:68` - **the three operative instruction documents still send their reader to the retired v3.1 file.** TECH_LEAD.md names it as the source for "CSS variables, fonts, SVG logomarks (reuse as-is, never redraw)" and REVIEWER.md's checklist still reads "Logomarks reused from `docs/vitrine/altarys-brand-identity-v3.1.html` as-is, never redrawn". Under D101 that file is obsolete in full: its colour section is the abandoned amber and teal palette under an ALTARYS ENTERPRISE heading, and its lockups carry the pre-D086 thin diamond. So the PR rewrites `CLAUDE.md` and the banner while leaving the two personality contracts pointing at exactly the trap it names. This is inside the item's own acceptance criterion 3, not outside its scope, so it belongs in this PR and needs no separate tracker. It is also load-bearing rather than theoretical: this round was seeded from REVIEWER.md and its line 70 instructed the reviewer to check the logomark against the obsolete document -> retire the three references to `CLAUDE.md`, `src/styles/tokens.css` and the design project, in the same terms the new banner uses.

### Important

- [ ] **[IMPORTANT]** `bin/og_images:125`, `:137`, `:141`, `:109-110` - **three of the template's colours are not the ones the site uses**, although the comment at lines 83 to 88 states "Les couleurs sont les valeurs de tokens.css" and lines 13 to 14 state the logomark is "toujours celui de `src/components/Logo.astro`". The diamond geometry does match `Logo.astro:45-64` value by value; the colours do not. `.labs` is `#C8922A` where `Logo.astro:174-177` puts `var(--gold-light)` on a dark surface, a rule `Logo.astro:160-162` spells out. `.accent`, the italic emphasis line and the most prominent element of the vignette, is `#C8922A` where the real hero's `em` is `var(--gold-light)` per `src/styles/global.css:214-217`. `.sous` is `#7C8AA3`, which exists nowhere in `tokens.css`; the hero subtitle is `var(--muted)`, `#9aa7be`, per `src/components/Hero.astro:138-141`. The trame alpha `.055` sits between `--gold-03` and `--gold-06`. None of this fails AA (measured: `#C8922A` on `#07111E` is 6.84:1, `#7C8AA3` is 5.42:1); the defect is that the share image renders duller than the page it advertises, and that the comment asserting otherwise is false -> `#E5B55A` for `.labs` and `.accent`, `#9AA7BE` for `.sous`, a real token alpha for the trame, or a comment that stops claiming token provenance.

- [ ] **[IMPORTANT]** `bin/og_images:96` - the font link requests `Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;**1,400**`. `BaseLayout.astro:91` loads `1,300` and stops there, and `CLAUDE.md` enumerates "Cormorant Garamond (300, 400, 600, italic 300)". The extra italic 400 is downloaded and never used: `.accent` is `font-style: italic` and inherits `font-weight: 300` from `.titre` -> drop `;1,400` so the generator loads exactly what the site loads.

- [ ] **[IMPORTANT]** `CLAUDE.md:88-91` - **"Re-run it after any change to the logomark" does not do what the sentence promises.** The copy half is true and is the item's real achievement: `fr.ts` and `en.ts` are read at generation time. The logomark half is not: the diamond is a hand-copied duplicate of `Logo.astro`'s SVG, hardcoded at `bin/og_images:154-164`. Re-running after a logomark change repaints the *old* geometry, which is exactly the drift that produced UI-005 in the first place -> either say plainly that the SVG is a duplicate that must be hand-updated alongside `Logo.astro`, or read the geometry from the component.

- [ ] **[IMPORTANT]** `docs/DECISIONS.md:109` - D100's rationale claims "Reading the copy from the dictionaries makes the whole class of drift impossible: a share preview can no longer say something the home page does not." The same commit contradicts it: the subtitle at `bin/og_images:71` and `:78` is hardcoded, and "Suites SaaS, conseil IT, RH et Finance, developpement sur mesure." is not what the home page says (`heroSubtitle` reads "Ressources humaines, finance d'entreprise et recouvrement de creances..."). Keeping the original subtitle is a founder call and is fine; the D-row overstating it is not, since a D-row is what the next reader trusts -> narrow the claim to the title and the emphasis line, or read `heroSubtitle` too.

- [ ] **[IMPORTANT]** `docs/work-items/UI-005.md` - acceptance criterion 3, "Each stays within its original weight budget. **Met**, 66 KB against 70 and 59 KB before", is false for the English image. Measured: `og-image.png` 71989 -> 67854 bytes, and `og-image-en.png` **60361 -> 68025 bytes**, up 12.7% against its own prior size. 66 Ko is a perfectly reasonable weight for a share image; the defect is grading the EN file against the FR file's budget and recording a "Met" it does not have -> restate honestly, or mark AC3 superseded the way AC4 already is.

- [ ] **[IMPORTANT]** `docs/reviews/UI-004-review.md` - **the review round this item names as its origin is not on the integration branch.** `docs/work-items/UI-FIX-003.md:8` and the commit message both cite round 2 of UI-004, but that file contains only `## Round 1` on `refonte-multipages` and on this branch. Round 2 exists solely on the unmerged tip `afeb4e8` of `feat/logo-maquette`, pushed after PR #33 had already merged. Once this lands, the log will show a fix for findings the durable record never mentions -> carry the round 2 section into `docs/reviews/UI-004-review.md` on this branch.

### Suggestions

- **[SUGGESTION]** `package.json` - `--check` is correct, deterministic and wired to nothing: no npm script, and the repository has no CI at all. Expose it, for instance `"og:check": "node bin/og_images --check"`, so it is discoverable by whoever next touches `Logo.astro` or the hero.
- **[SUGGESTION]** `bin/og_images:59-60` - the dictionary paths are relative, so a run from anywhere but the repository root dies on a raw `ENOENT` stack trace. The rest of the file is careful about its failure messages; this path is not.
- **[SUGGESTION]** `bin/og_images:206-209` - the comment block and the `rmSync` call are dedented to column 0 inside the `if`, unlike the identical block at 286 to 289.
- **[SUGGESTION]** `src/layouts/BaseLayout.astro` - no `og:image:alt` or `twitter:image:alt` is emitted. Optional, but this is the item where adding it from the dictionaries would cost one line.
- **[SUGGESTION]** `docs/work-items/UI-FIX-003.md:3` - this item is typed `FIX` while it retypes UI-005 to `STORY` on the ground that "a visitor does see the difference" in a share preview. Same visitor-visible change, two answers to the decision tree's first question. No harm here, since a `FIX` is not excused the visual check, but the two lines sit in the same commit.

### Correctness (code-review skill)

Ran as `code-review:code-review` on PR #35: eligibility check, CLAUDE.md audit, shallow bug scan, git-history pass, prior-PR-comment pass and comment-accuracy pass. Its two highest-value findings, the D100 collision and the hardcoded logomark duplicate, are promoted above as a blocker and an important; its palette findings are folded into the colour item. What remains is hardening on the new script, each confirmed against the code but none of it reachable in the normal path, so it stays non-blocking.

- **[SUGGESTION]** `bin/og_images:180-290` - nothing wraps the run in `try`/`finally`. `chrome.kill()` and `rmSync(profil)` execute only on the two anticipated branches; the abort at 260 to 265 already leaks the temp profile, and any throw between the spawn and the end leaves a live Chrome process behind as well.
- **[SUGGESTION]** `bin/og_images:218-232` - `envoyer` resolves on `m.result` and ignores `m.error`, and never rejects or times out. A CDP error surfaces as `Buffer.from(undefined, 'base64')` throwing a `TypeError` at line 270; a Chrome death mid-render hangs the script forever instead of exiting non-zero. Same for the unbounded `ws.onopen` wait at 215 to 216, which has no counterpart to the 60-attempt bound used just above it.
- **[SUGGESTION]** `bin/og_images:28-39` and `:180` - `process.env.CHROME` bypasses the `existsSync` filter, so an override pointing at a missing binary passes the `if (!CHROME)` guard, and `spawn` has no `'error'` listener to catch the resulting `ENOENT`.
- **[SUGGESTION]** `bin/og_images:178` - the debug port is `9500 + pid % 400` with no availability check, so two concurrent runs, or one run against a not-yet-released port from a killed predecessor, can attach to the wrong target rather than fail clearly.
- **[SUGGESTION]** `bin/og_images:248-255` - `Page.navigate`'s acknowledgement is not a guarantee that the new document's execution context has replaced the old one, so `document.fonts.ready` can in principle be evaluated against the previous page. Not observed: three consecutive `--check` runs were byte-identical, so this stays unconfirmed. Waiting on `Page.loadEventFired` would remove the doubt the surrounding comment claims to have removed.

### Checked and clean

Bilingual parity untouched: no route, no dictionary key, no `Dictionary` derivation and no `BaseLayout.astro` change; hreflang, canonical, OG and Twitter tags, `fr_CI` and `en` locales, JSON-LD and the 26-entry sitemap all verified in `dist`. Both OG images are present in `public/` and in `dist/`. Editorial guardrails hold: no price, no client name, no date, no invented figure. Palette holds: navy and gold only; the amber, teal and ALTARYS ENTERPRISE strings appear only where the new banner and D101 name them as traps, which is the point. Deployment untouched: `output: 'static'`, `./dist`, no adapter, no secret, PR targets `refonte-multipages`.

### Summary

The generator is real work and it holds up: deterministic to the byte, copy read from the dictionaries, and the two images it produces are correct today. What blocks is around it. D100 is already occupied by FORM-001, which merged before this PR opened, and GitHub already reports the conflict. The font guard the script advertises does not detect a font outage, and a run without network silently ships a Times New Roman share image. And the PR retires the v3.1 brand file everywhere except in the two personality documents that actually send authors and reviewers to it.
