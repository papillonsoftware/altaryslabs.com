---
import BaseLayout from '~/layouts/BaseLayout.astro';
import ProductsHub from '~/components/ProductsHub.astro';

const locale = 'en' as const;
const pageKey = 'products' as const;
---

<BaseLayout locale={locale} pageKey={pageKey}>
  <ProductsHub locale={locale} />
</BaseLayout>
