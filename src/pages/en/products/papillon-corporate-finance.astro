---
import BaseLayout from '~/layouts/BaseLayout.astro';
import PageScaffold from '~/components/PageScaffold.astro';

const locale = 'en' as const;
const pageKey = 'productsFinance' as const;
---

<BaseLayout locale={locale} pageKey={pageKey}>
  <PageScaffold locale={locale} pageKey={pageKey} parent="products" />
</BaseLayout>
